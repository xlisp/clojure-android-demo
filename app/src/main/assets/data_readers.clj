;; Loaded by DalvikDynamicClassLoader.getDataReadersStream() from app assets.
;; (See the patched clojure.core/load-data-readers.) Empty map = no custom
;; tagged-literal readers; add entries like  {my/tag my.ns/reader-fn}  here.
{}
